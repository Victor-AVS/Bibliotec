services:
  - type: web
    name: Bibliotec
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 3 --bind 0.0.0.0:$PORT --timeout 30 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
